// Others
// import * as DataHandler from '@data';

// Helpers
import { d, empty, flipOnKey, makeList } from "@utils";

//--------------------------------------------------------------
export default class Master {
  /* static async premise(): Promise<any> {
        try {
            return {
                data: DataHandler.Premise,
                dataOnKey: flipOnKey(DataHandler.Premise, 'key'),
                keys: makeList(DataHandler.Premise, 'key'),
            };

        } catch (e) {return {error: e}}
    }
    */
}
