server:
  port: ${PORT:5000}
spring:
  datasource:
    # url: ${DB_URL}
    # username: ${DB_USERNAME}
    # password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  application:
    name: ClickShop
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
# App
app:
  name: ClickShop
  # url: ${APP_URL}

# jwt:
  # secret: ${JWT_SECRET}
  # secret: mMZJH4r9K2fJ0XK6uXJbK3KJpXc7JtJxqN4v7FJQkQw=mMZJH4r9K2fJ0XK6uXJbK3KJpXc7JtJxqN4v7FJQkQw=

# # Stripe
# stripe:
#   success: ${STRIPE_SUCCESS}
#   cancel: ${STRIPE_CANCEL}
#   public-key: ${STRIPE_PUBLIC_KEY}
#   secret-key: ${STRIPE_SECRET_KEY}
